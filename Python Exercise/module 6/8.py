# Dogello, RJ-Mel B.
#BSIT 2D

#MODULE 6

#Create a program that defines a tuple of ages and finds the minimum and maximum age, then prints both.

age = (25, 30, 22, 35, 18)

if min(age) == 18:
    print(f"Minimum age: {min(age)}")
if max(age) == 35:
     print(f"Maximum age: {max(age)}")  
